version: "2"
sql:
  - schema: "./db/migration/"
    queries: "./db/query/"
    engine: "postgresql"
    gen:
      go:
        package: "authors"
        out: "./db/sqlc/"
    # database:
    #   managed: true
    rules:
      - sqlc/db-prepare
